# Local secret scan — install once per clone:
#   pip install pre-commit && pre-commit install
# Then every `git commit` runs gitleaks on the staged diff.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
